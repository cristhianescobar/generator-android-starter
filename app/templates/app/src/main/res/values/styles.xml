<resources>

  <!-- Base application theme. -->

  <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    <!-- Set AppCompat’s actionBarStyle -->
    <item name="actionBarStyle">@style/AppTheme.ActionBarStyle</item>
    <item name="drawerArrowStyle">@style/AppTheme.DrawerArrowStyle</item>

    <!-- Set AppCompat’s color theming attrs -->
    <item name="colorPrimary">@color/primary</item>
    <item name="colorPrimaryDark">@color/primary_dark</item>
    <item name="colorAccent">@color/accent</item>

    <!-- The rest of your attributes -->
    <item name="android:windowBackground">@color/window_background</item>
  </style>

  <!-- App Toolbar -->

  <style name="AppTheme.ActionBarStyle" parent="ThemeOverlay.AppCompat.ActionBar">
    <item name="android:background">@color/action_bar_background</item>
  </style>

  <style name="AppTheme.DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
    <item name="spinBars">true</item>
    <item name="color">@color/dark_gray</item>
  </style>

  <!-- Buttons -->

  <style name="AppTheme.Button.Flat" parent="Widget.AppCompat.ActionButton">
    <item name="android:background">@android:color/transparent</item>
    <item name="android:textAllCaps">false</item>
    <item name="android:textColor">@color/flat_button_text</item>
    <item name="android:textSize">24sp</item>
  </style>

</resources>
